<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
"http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="stamp">
	<!-- Main -->
<!-- 	<select id="slider" resultType="realfarm.spring.gallery.GalleryVo">
		SELECT url 
		FROM picture 
		ORDER BY pnum DESC 
		limit 6;
	</select> -->
	
	<select id="searchAccount" parameterType="String" resultType="com.stamp.member.LoginVo">
		SELECT id, password
		FROM account
		WHERE id=#{id}
	</select>
	
	<insert id="saveStamp" parameterType="com.stamp.stamp.StampVo">
		INSERT INTO stamp(stampNum)
		VALUES (#{stampNUM})
	</insert>
	
	<select id="searchStamp" parameterType="int" resultType="com.stamp.stamp.StampVo">
		SELECT Sid, stampNUM
		FROM stamp
		WHERE Sid=#{Sid}
	</select>
	
	<select id="searchFestival" resultType="com.stamp.data.FestivalVo">
		SELECT title, link, organ, period, tel, description
		FROM festival
		WHERE substring(period, 4, 1)=6
	</select>
	
	<!-- 
	<delete id="deleteCalendar" parameterType="int">
		DELETE FROM calendar
		WHERE snum=#{num}
	</delete> -->
   
</mapper>